version: '3.8'

services:
  hedge-fund-app:
    build: .
    ports:
      - "3000:80"
    container_name: quantum-capital-website
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro 